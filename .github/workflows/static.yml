name: Build and Publish basher to GitHub Pages

on:
  push:
    branches: [ 'master' ]
    # Trigger the action only when files change in the folders defined here.
    paths: ['src/**', 'public/**']
  # Allows workflow to be run manually from the Actions tab on GitHub.
  workflow_dispatch:

# Allow this job to clone the repo and create a page deployment
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.1

      #- name: Setup Node
        #uses: actions/setup-node@v3
        #with:
          #node-version: 20

      - name: Astro build
        uses: withastro/action@v2.0.0
        with:
          package-manager: npm

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4.0.5
