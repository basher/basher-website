---
import type { HTMLTag } from 'astro/types';
import { Image } from 'astro:assets';
import star from '../images/star.webp';

interface Props {
    headingLevel: string;
    headingText: string;
    id?: string;
    tabindex?: string;
}

const { headingLevel, headingText, id, tabindex } = Astro.props;
const HeadingElement = `h${headingLevel}` as HTMLTag;
---

<HeadingElement class:list={['title wrapper wrapper--fit-content']} id={id} tabindex={tabindex}>
    <Image src={star} alt=""/>
    <span>{headingText}</span>
</HeadingElement>

<style>
    .title {
        align-items: center;
        display: flex;
        gap: var(--gutter-s);
    }

    .title img {
        aspect-ratio: 1;
        width: 1.25em;
    }
</style>
